import { FC } from 'react';

import styled from 'styled-components';

const QuestionSection = styled.section``;

const Question: FC = () => {
  return <QuestionSection></QuestionSection>;
};

export default Question;
